<!DOCTYPE html>
<html>
<head>
<%-include("../../Views/Partials/user/header")%>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: #f5f5f5;
            min-height: 100vh;
            justify-content: center;
            align-items: center;
        }

        .wallet-container {
            margin: 85px;
           margin-left: 300px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 800px;
            padding: 25px;
        }

        .top-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid #eee;
        }

        .wallet-logo {
            font-size: 1.5em;
            font-weight: bold;
            color: #232f3e;
        }

        .main-balance {
            text-align: center;
            padding: 20px;
            background: #f7f7f7;
            border-radius: 10px;
            margin-bottom: 25px;
        }

        .balance-amount {
            font-size: 2.5em;
            font-weight: bold;
            color: #232f3e;
            margin: 10px 0;
        }

        .quick-actions {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 15px;
            margin-bottom: 30px;
        }

        .action-card {
            background: white;
            border: 1px solid #eee;
            border-radius: 10px;
            padding: 15px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .action-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .action-icon {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .tabs {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
            border-bottom: 1px solid #eee;
        }

        .tab {
            padding: 10px 20px;
            cursor: pointer;
            border-bottom: 2px solid transparent;
        }

        .tab.active {
            border-bottom: 2px solid #00a884;
            font-weight: bold;
        }

        .payment-methods {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin-bottom: 25px;
        }

        .payment-card {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 15px;
            display: flex;
            align-items: center;
            gap: 15px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .payment-card:hover {
            background: #eee;
        }

        .card-icon {
            width: 50px;
            height: 50px;
            background: white;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        .transactions {
            max-height:40vh;
            background: white;
            border-radius: 10px;
            overflow: scroll;
        }

        .transaction {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            border-bottom: 1px solid #eee;
            transition: background 0.3s ease;
        }

        .transaction:hover {
            background: #f8f9fa;
        }

        .transaction-left {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .transaction-icon {
            width: 40px;
            height: 40px;
            background: #f1f2f6;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .btn {
            background: #00a884;
            background: -webkit-linear-gradient(to right, #00a884, #009688, #00897b, #00796b);
            background: linear-gradient(to right, #00a884, #009688, #00897b, #00796b);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1em;
            transition: all 0.3s ease;
        }

        .btn:hover {
            background: #27ae60;
        }

        .btn-outline {
            background: transparent;
            border: 1px solid #232f3e;
            color: #232f3e;
        }

        .btn-outline:hover {
            background: #232f3e;
            color: white;
        }

        .amount {
            font-weight: bold;
        }

        .positive {
            color: #00b894;
        }

        .negative {
            color: #d63031;
        }

        .quick-pay {
            background: #fbfbfb;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 25px;
        }

        .quick-pay-title {
            margin-bottom: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        /* .reward-banner {
            background: linear-gradient(135deg, #232f3e 0%, #131921 100%);
            color: white;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        } */
    </style>
</head>
<body>
    <div class="wallet-container">
        <!-- <div class="top-nav">
            <div class="wallet-logo">Fashion Pay</div>
            <button class="btn btn-outline" onclick="showSettings()">⚙️ Settings</button>
        </div> -->

        <div class="main-balance">
            <div>Available Balance</div>
            <div class="balance-amount">₹<%=wallet.balanceAmount.toFixed(2)%></div>
            <!-- <button class="btn" onclick="addMoney()">Add Money</button> -->
        </div>

        <!-- <div class="reward-banner">
            <div>
                <h3>Cashback Rewards</h3>
                <p>Earn 5% back on fashion purchases</p>
            </div>
            <button class="btn btn-outline">View Rewards</button>
        </div> -->
<!-- 
        <div class="quick-actions">
            <div class="action-card" onclick="sendMoney()">
                <div class="action-icon">↗️</div>
                <div>Send</div>
            </div>
            <div class="action-card" onclick="requestMoney()">
                <div class="action-icon">↙️</div>
                <div>Request</div>
            </div>
            <div class="action-card" onclick="scanQR()">
                <div class="action-icon">📷</div>
                <div>Scan QR</div>
            </div>
            <div class="action-card" onclick="showOffers()">
                <div class="action-icon">🏷️</div>
                <div>Offers</div>
            </div>
        </div> -->

        <!-- <div class="quick-pay">
            <div class="quick-pay-title">
                <h3>Quick Pay</h3>
                <button class="btn btn-outline">Manage</button>
            </div>
            <div class="payment-methods">
                <div class="payment-card">
                    <div class="card-icon">💳</div>
                    <div>
                        <div>VISA ending in 4589</div>
                        <small style="color: #636e72">Default payment method</small>
                    </div>
                </div>
                <div class="payment-card">
                    <div class="card-icon">🏦</div>
                    <div>
                        <div>Bank Account</div>
                        <small style="color: #636e72">Connected to wallet</small>
                    </div>
                </div>
            </div>
        </div> -->

        <div class="tabs">
            <div class="tab active" onclick="showTab('transactions')">Transactions</div>
            <!-- <div class="tab" onclick="showTab('pending')">Pending</div> -->
            <!-- <div class="tab" onclick="showTab('offers')">Offers</div> -->
        </div>

        <div class="transactions">
            <% if (transactions.length === 0) { %>
                <p>No transactions yet.</p>
            <% } else { %>
                <% transactions.slice().reverse().forEach(transaction => { %> <!-- Reverse the array -->
                    <div class="transaction">
                        <div class="transaction-left">
                            <div>
                                <div><%= transaction.description %></div>
                                <small><%= new Date(transaction.date).toLocaleString() %></small>
                            </div>
                        </div>
                        <div class="amount <%= transaction.type === "credit" ? "positive" : "negative" %>">
                            <%= transaction.type === "credit" ? "+" : "-" %>₹<%= transaction.amount %>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>

    <script>
        // Tab functionality
        function showTab(tabName) {
            document.querySelectorAll('.tab').forEach(tab => {
                tab.classList.remove('active');
            });
            event.target.classList.add('active');
            // Add tab switching logic here
        }

        // Action functions
        function addMoney() {
            alert("Add Money functionality will be implemented here");
        }

        function sendMoney() {
            alert("Send Money functionality will be implemented here");
        }

        function requestMoney() {
            alert("Request Money functionality will be implemented here");
        }

        function scanQR() {
            alert("Scan QR functionality will be implemented here");
        }

        function showOffers() {
            alert("Offers functionality will be implemented here");
        }

        function showSettings() {
            alert("Settings functionality will be implemented here");
        }

        // Add hover effects to interactive elements
        document.querySelectorAll('.payment-card, .transaction').forEach(element => {
            element.addEventListener('mouseover', () => {
                element.style.transform = 'translateX(5px)';
                element.style.transition = 'transform 0.3s ease';
            });
            element.addEventListener('mouseout', () => {
                element.style.transform = 'translateX(0)';
            });
        });
    </script>
</body>
</html>

<%-include("../../Views/Partials/user/footer")%>
